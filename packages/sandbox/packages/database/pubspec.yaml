name: database
description: Database layer
publish_to: none

environment:
  sdk: '>=3.10.0 <4.0.0'
resolution: workspace
dependencies:
  drift: ^2.30.0
  rxdart: ^0.28.0
  sqlite3_flutter_libs: ^0.5.41

dev_dependencies:
  build_runner: any
  drift_dev: ^2.30.0
